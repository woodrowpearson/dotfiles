# Automatic font installation

After installing iterm2 you can install the required fonts for the powerlevel10k theme by typing `p10k configure` Simply answer Yes when asked whether to install Meslo Nerd Font.
